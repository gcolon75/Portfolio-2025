# Amadeus Assets

## Current Status
❌ No assets currently present. This project uses external GitHub link instead.

## Expected Assets (Optional)
If you want to add visual assets for this project:

```
/public/assets/projects/Amadeus/
├── amadeus-cover.jpg              # Hero/cover image (recommended: 1200x675px, 16:9)
├── amadeus-thumb.png              # Card thumbnail (recommended: 400x400px)
├── screenshot-1.png               # Discord bot interface screenshot
├── screenshot-2.png               # Alert notification example
└── amadeus-demo.pdf               # Optional: Documentation or demo slides
```

## Usage
Once added, update `src/data/projects.js`:
- Set `coverImage` or add to `assets.images` array for thumbnails
- Add to `assets.pdfs` array for documentation

## Current Configuration
The project currently references:
- External GitHub repository link
- No local assets configured
